---
import Layout from '../layouts/Layout.astro';

// Dados dos projetos reais da MLTech
const projects = [
  {
    id: 1,
    title: "Sistema de Inventário de Estoque",
    slug: "sistema-inventario-estoque",
    category: "Web",
    description: "Aplicação web para contagem de mercadorias e inventário de estoque por localização, com leitura de códigos de barras e exportação de planilhas para integração com ERP.",
    technologies: ["Python", "Flask", "Pandas", "SQLAlchemy"],
    image: "/api/placeholder/400/300",
    features: ["Contagem por localização", "Leitura de códigos de barras", "Exportação de planilhas", "Integração com ERP"],
    client: "Empresa Logística",
    duration: "2 meses",
    year: "2024",
    impact: "Aumento na produtividade durante inventário, economia de tempo e redução de divergências"
  },
  {
    id: 2,
    title: "Automação de Simulação de Crédito Imobiliário",
    slug: "automacao-credito-imobiliario",
    category: "Automação",
    description: "Automação para simulação simultânea em 4 sites bancários diferentes, utilizando um único formulário e entregando resultados tabelados para comparação.",
    technologies: ["Python", "Flask", "Selenium"],
    image: "/api/placeholder/400/300",
    features: ["Simulação simultânea em 4 bancos", "Formulário único", "Execução sem interface visual", "Relatórios comparativos"],
    client: "Corretora Imobiliária",
    duration: "3 meses",
    year: "2024",
    impact: "Aumento de 4x na produtividade das simulações"
  },
  {
    id: 3,
    title: "Integração entre ERPs Magazord x Posthaus",
    slug: "integracao-magazord-posthaus",
    category: "Integração",
    description: "Integração de produtos, estoques, pedidos e notas fiscais entre duas plataformas de vendas online através de FTP e API REST.",
    technologies: ["Python", "Flask", "Cron Jobs", "FTP", "API REST"],
    image: "/api/placeholder/400/300",
    features: ["Integração de produtos e estoques", "Sincronização de pedidos", "Notas fiscais automáticas", "Processamento via cron jobs"],
    client: "E-commerces",
    duration: "2 meses",
    year: "2023",
    impact: "Aumento de R$ 300.000/mês no faturamento"
  }
];

const categories = ["Todos", "Web", "Automação", "Integração"];
---

<Layout title="Portfólio - MLTech" description="Conheça alguns dos projetos que desenvolvemos para nossos clientes. Soluções inovadoras em tecnologia.">
  <!-- Hero Section -->
  <section class="bg-gradient-to-br from-gray-900 to-gray-800 text-white py-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center">
        <h1 class="text-4xl md:text-5xl font-bold mb-6">
          Nosso <span class="text-blue-400">Portfólio</span>
        </h1>
        <p class="text-xl text-gray-300 max-w-3xl mx-auto">
          Projetos que transformaram negócios e impulsionaram resultados
        </p>
      </div>
    </div>
  </section>

  <!-- Filter Tabs -->
  <section class="py-8 bg-white border-b">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex flex-wrap justify-center gap-4">
        {categories.map((category) => (
          <button
            class={`px-6 py-2 rounded-full text-sm font-medium transition-colors duration-200 ${
              category === "Todos" 
                ? "bg-blue-600 text-white" 
                : "bg-gray-100 text-gray-600 hover:bg-gray-200"
            }`}
            data-category={category}
          >
            {category}
          </button>
        ))}
      </div>
    </div>
  </section>

  <!-- Projects Grid -->
  <section class="py-20 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {projects.map((project) => (
          <div class="bg-white rounded-xl shadow-sm hover:shadow-lg transition-all duration-300 overflow-hidden group" data-category={project.category}>
            <div class="h-48 bg-gradient-to-br from-blue-100 to-purple-100 flex items-center justify-center relative overflow-hidden">
              <div class="text-center">
                <div class="w-16 h-16 bg-white rounded-lg flex items-center justify-center mx-auto mb-2 shadow-md">
                  <span class="text-2xl font-bold text-blue-600">
                    {project.category === "Web" ? "🌐" : 
                     project.category === "Automação" ? "⚡" : 
                     project.category === "Integração" ? "🔗" : "💻"}
                  </span>
                </div>
                <span class="text-sm font-medium text-gray-600 bg-white px-3 py-1 rounded-full shadow-sm">
                  {project.category}
                </span>
              </div>
              <div class="absolute top-4 right-4 bg-white px-2 py-1 rounded-full text-xs font-medium text-gray-600 shadow-sm">
                {project.year}
              </div>
            </div>
            <div class="p-6">
              <div class="flex justify-between items-start mb-2">
                <h3 class="text-xl font-bold text-gray-900 group-hover:text-blue-600 transition-colors duration-200">
                  {project.title}
                </h3>
              </div>
              
              <p class="text-gray-600 text-sm mb-4 line-clamp-3">{project.description}</p>
              
              <div class="mb-4">
                <h4 class="text-sm font-semibold text-gray-900 mb-2">Tecnologias:</h4>
                <div class="flex flex-wrap gap-1">
                  {project.technologies.map((tech) => (
                    <span class="px-2 py-1 bg-blue-100 text-blue-600 text-xs rounded-full">
                      {tech}
                    </span>
                  ))}
                </div>
              </div>

              <div class="mb-4">
                <h4 class="text-sm font-semibold text-gray-900 mb-2">Principais funcionalidades:</h4>
                <ul class="text-sm text-gray-600 space-y-1">
                  {project.features.slice(0, 3).map((feature) => (
                    <li class="flex items-center">
                      <svg class="w-3 h-3 text-green-500 mr-2 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                      </svg>
                      {feature}
                    </li>
                  ))}
                  {project.features.length > 3 && (
                    <li class="text-gray-500 text-xs">
                      +{project.features.length - 3} outras funcionalidades
                    </li>
                  )}
                </ul>
              </div>

              <div class="mb-4 text-xs text-gray-500 space-y-1">
                <div class="flex justify-between">
                  <span>Cliente:</span>
                  <span class="font-medium">{project.client}</span>
                </div>
                <div class="flex justify-between">
                  <span>Duração:</span>
                  <span class="font-medium">{project.duration}</span>
                </div>
                <div class="mt-2 p-2 bg-green-50 rounded-lg">
                  <div class="text-green-700 font-medium text-xs">Impacto:</div>
                  <div class="text-green-600 text-xs">{project.impact}</div>
                </div>
              </div>

              <a 
                href={`/portfolio/${project.slug}`}
                class="w-full bg-blue-600 text-white py-2 px-4 rounded-lg text-sm font-medium hover:bg-blue-700 transition-colors duration-200 inline-block text-center group-hover:shadow-md"
              >
                Ver Detalhes Completos
              </a>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Stats Section -->
  <section class="py-20 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
          Números que Falam
        </h2>
        <p class="text-xl text-gray-600 max-w-2xl mx-auto">
          Resultados que comprovam nossa qualidade e dedicação
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
        <div class="text-center">
          <div class="text-4xl font-bold text-blue-600 mb-2">50+</div>
          <div class="text-gray-600">Projetos Entregues</div>
        </div>
        <div class="text-center">
          <div class="text-4xl font-bold text-green-600 mb-2">100%</div>
          <div class="text-gray-600">Clientes Satisfeitos</div>
        </div>
        <div class="text-center">
          <div class="text-4xl font-bold text-purple-600 mb-2">5+</div>
          <div class="text-gray-600">Anos de Experiência</div>
        </div>
        <div class="text-center">
          <div class="text-4xl font-bold text-orange-600 mb-2">24/7</div>
          <div class="text-gray-600">Suporte Disponível</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Testimonials -->
  <section class="py-20 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
          O que Nossos Clientes Dizem
        </h2>
        <p class="text-xl text-gray-600 max-w-2xl mx-auto">
          Depoimentos de quem já trabalhou conosco
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <div class="bg-white p-6 rounded-xl shadow-sm">
          <div class="flex items-center mb-4">
            <div class="w-12 h-12 bg-blue-100 rounded-full flex items-center justify-center">
              <span class="text-blue-600 font-bold">J</span>
            </div>
            <div class="ml-4">
              <h4 class="font-semibold text-gray-900">João Silva</h4>
              <p class="text-sm text-gray-600">CEO, TechCorp</p>
            </div>
          </div>
          <p class="text-gray-600 text-sm">
            "A MLTech transformou completamente nosso processo de vendas. O sistema que desenvolveram 
            aumentou nossa eficiência em 40%."
          </p>
        </div>

        <div class="bg-white p-6 rounded-xl shadow-sm">
          <div class="flex items-center mb-4">
            <div class="w-12 h-12 bg-green-100 rounded-full flex items-center justify-center">
              <span class="text-green-600 font-bold">M</span>
            </div>
            <div class="ml-4">
              <h4 class="font-semibold text-gray-900">Maria Santos</h4>
              <p class="text-sm text-gray-600">Diretora, Inovação Ltda</p>
            </div>
          </div>
          <p class="text-gray-600 text-sm">
            "Profissionais excepcionais! Entregaram nosso e-commerce no prazo e com qualidade superior 
            ao esperado."
          </p>
        </div>

        <div class="bg-white p-6 rounded-xl shadow-sm">
          <div class="flex items-center mb-4">
            <div class="w-12 h-12 bg-purple-100 rounded-full flex items-center justify-center">
              <span class="text-purple-600 font-bold">C</span>
            </div>
            <div class="ml-4">
              <h4 class="font-semibold text-gray-900">Carlos Oliveira</h4>
              <p class="text-sm text-gray-600">Gerente, AutoCorp</p>
            </div>
          </div>
          <p class="text-gray-600 text-sm">
            "A automação que criaram para nossos relatórios nos economizou horas de trabalho manual. 
            Recomendo de olhos fechados!"
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-20 bg-blue-600">
    <div class="max-w-4xl mx-auto text-center px-4 sm:px-6 lg:px-8">
      <h2 class="text-3xl md:text-4xl font-bold text-white mb-6">
        Quer ser nosso próximo caso de sucesso?
      </h2>
      <p class="text-xl text-blue-100 mb-8">
        Entre em contato conosco e vamos transformar sua ideia em realidade.
      </p>
      <a
        href="/contato"
        class="bg-white text-blue-600 px-8 py-3 rounded-lg text-lg font-semibold hover:bg-blue-50 transition-colors duration-200 inline-block"
      >
        Iniciar Projeto
      </a>
    </div>
  </section>
</Layout>

<script>
  // Filter functionality
  document.addEventListener('DOMContentLoaded', () => {
    const filterButtons = document.querySelectorAll('[data-category]');
    const projectCards = document.querySelectorAll('[data-category]');
    
    filterButtons.forEach(button => {
      button.addEventListener('click', () => {
        const category = button.getAttribute('data-category');
        
        // Update active button
        filterButtons.forEach(btn => {
          (btn as HTMLElement).classList.remove('bg-blue-600', 'text-white');
          (btn as HTMLElement).classList.add('bg-gray-100', 'text-gray-600');
        });
        button.classList.remove('bg-gray-100', 'text-gray-600');
        button.classList.add('bg-blue-600', 'text-white');
        
        // Filter projects
        projectCards.forEach(card => {
          if (category === 'Todos' || card.getAttribute('data-category') === category) {
            (card as HTMLElement).style.display = 'block';
          } else {
            (card as HTMLElement).style.display = 'none';
          }
        });
      });
    });
  });
</script>

